import { Box, styled } from "@mui/material";

const LessonMainContent = styled(Box)(() => ({
  width: "100%",
  maxWidth: "962px",
  marginLeft: "auto",
  marginRight: "auto",
  flexGrow: 1,
}));

export default LessonMainContent;
